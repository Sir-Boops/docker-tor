pipeline:
    docker:
        image: plugins/docker
        dockerfile: Dockerfile
        repo: sirboops/tor
        tags: [0.4.0.5, latest]
        experimental: true
        squash: true
        secrets: [ docker_username, docker_password ]
