pipeline:
    docker:
        image: plugins/docker
        dockerfile: Dockerfile
        repo: sirboops/tor
        tags: [0.3.5.8, latest]
        experimental: true
        squash: true
        secrets: [ docker_username, docker_password ]
